using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class OffsetBlackKing : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //this.transform.position += new Vector3(-0.007142f, 0, 0.0732857f);
        this.transform.position += new Vector3(-0.0056428f, 0, 0.0793857f);
    }
}
